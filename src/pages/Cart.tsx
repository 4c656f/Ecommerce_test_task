import React, {FC} from 'react';

type CartProps = {}

const Cart: FC<CartProps> = (props: CartProps) => {

    const {} = props


    return (
        <div>

        </div>
    );
};

export default Cart;